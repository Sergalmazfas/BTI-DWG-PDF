# 🤖 Отчет о переписывании BTI-Bot под поэтапный DWG-first режим

## 🎯 **Задача выполнена**

Переписан BTI-Bot для работы в поэтапном режиме с приоритетом DWG файлов и блокировкой задач.

---

## ✅ **Реализованная функциональность**

### **1. Блокировка задач (Lock System)**
- ✅ **Firestore блокировки** с TTL 30 минут
- ✅ **Одна задача за раз** - проверка активных задач
- ✅ **Автоматическое снятие** при завершении/ошибке
- ✅ **Сообщение при занятом lock:** "⚠️ Сейчас в работе проект <JOB_ID>. Новый файл приму, когда закончу."

### **2. Поэтапный workflow**
```
IDLE → WAIT_DWG → PROCESSING_DWG → DWG_READY → ASK_PDF → (PDF_CONVERT | DONE_NO_PDF) → DONE → IDLE
```

### **3. Команда /bti**
- ✅ **Описание этапов** без автопреобразования
- ✅ **Фокус на DWG** - "Вы получите ГЛАВНОЕ: готовый DWG для доработок"
- ✅ **Просьба прислать DWG** файл

### **4. Обработка файлов**
- ✅ **Валидация DWG** - mime type, расширение .dwg, размер > 0
- ✅ **Сохранение в GCS:** `gs://btibot-processed/raw/{chat_id}/{job_id}/input.dwg`
- ✅ **Сообщение:** "✅ Файл принят. Запускаю обработку в режиме БТИ (Autodesk API)…"

### **5. AutoDesk Forge API интеграция**
- ✅ **Только Forge API** - никаких локальных конвертаций
- ✅ **WorkItem создание** с шаблоном BTI_Template.dwt
- ✅ **Мониторинг статуса** каждые 30 секунд
- ✅ **Таймаут 15 минут** с отменой WorkItem

### **6. Готовый DWG**
- ✅ **Сохранение:** `gs://btibot-processed/ready/{chat_id}/{job_id}/out.dwg`
- ✅ **Отправка файла** + ссылка пользователю
- ✅ **Сообщение:** "🏁 Готово. Ваш DWG: <link>"
- ✅ **Вопрос о PDF** с inline кнопками

### **7. Выбор PDF**
- ✅ **Inline кнопки:** [Сделать PDF] / [Не нужно]
- ✅ **PDF YES:** конвертация через Forge API
- ✅ **PDF NO:** "👌 Ок, оставляем только DWG. Задача завершена."
- ✅ **Lock держится** до финального решения

### **8. Обработка ошибок**
- ✅ **WorkItem ошибки:** "⛔ Ошибка: не удалось создать WorkItem в Autodesk API"
- ✅ **Таймауты:** отмена WorkItem, уведомление пользователя
- ✅ **Сохранение исходного файла** с ссылкой
- ✅ **Автоматическое unlock** при ошибках

---

## 📁 **Структура файлов**

### **Созданные файлы:**
- `bti_bot_dwg_first.py` - основной код бота
- `Dockerfile.bti-dwg-first` - Docker конфигурация
- `deploy_bti_dwg_first.sh` - скрипт деплоя

### **Пути в GCS:**
```
gs://btibot-processed/
├── templates/
│   └── BTI_Template.dwt          # Шаблон БТИ
├── raw/
│   └── {chat_id}/
│       └── {job_id}/
│           └── input.dwg         # Входной файл
└── ready/
    └── {chat_id}/
        └── {job_id}/
            ├── out.dwg           # Готовый DWG
            └── out.pdf           # Готовый PDF (опционально)
```

---

## 🔧 **Переменные окружения**

```bash
FORGE_CLIENT_ID=from_secret_manager
FORGE_CLIENT_SECRET=from_secret_manager
BOT_TOKEN=from_secret_manager
GCS_BUCKET=btibot-processed
AUTO_PDF=false                    # Важно: убираем автоконвертацию
JOB_TIMEOUT_SEC=900               # 15 минут
GOOGLE_CLOUD_PROJECT=talkhint
```

---

## 📱 **Точные тексты сообщений**

### **При занятом lock:**
```
⚠️ Сейчас в работе проект <JOB_ID>. Новый файл приму, когда закончу.
```

### **Принятие файла:**
```
✅ Файл принят. Запускаю обработку в режиме БТИ (Autodesk API)…
```

### **Готов DWG:**
```
🏁 Готово. Ваш DWG: <link>
```

### **Вопрос о PDF:**
```
Нужен ли PDF (A4, Landscape)?
[Сделать PDF] [Не нужно]
```

### **PDF готов:**
```
📄 PDF готов: <link>
```

### **Завершено без PDF:**
```
👌 Ок, DWG достаточно. Задача завершена.
```

### **Ошибка:**
```
⛔ Ошибка обработки. Исходный DWG сохранён: <raw link>
```

---

## 🚀 **Деплой**

### **Команда деплоя:**
```bash
./deploy_bti_dwg_first.sh
```

### **Ручной деплой:**
```bash
gcloud run deploy bti-bot-dwg-first \
  --image gcr.io/talkhint/bti-bot-dwg-first:latest \
  --region europe-west1 \
  --platform managed \
  --allow-unauthenticated \
  --set-env-vars GOOGLE_CLOUD_PROJECT=talkhint,GCS_BUCKET=btibot-processed,AUTO_PDF=false,JOB_TIMEOUT_SEC=900 \
  --set-secrets FORGE_CLIENT_ID=FORGE_CLIENT_ID:latest,FORGE_CLIENT_SECRET=FORGE_CLIENT_SECRET:latest,BOT_TOKEN=BOT_TOKEN:latest
```

---

## ✅ **Критерии приёмки**

### **Все критерии выполнены:**
- ✅ **Бот никогда не делает PDF без явного согласия** (кнопка)
- ✅ **После загрузки всегда сначала присылается готовый DWG**
- ✅ **Пока проект не завершён** (PDF/отказ) — бот не принимает новые файлы (lock)
- ✅ **Ошибки Forge не ломают очередь**, lock корректно снимается

---

## 🎯 **Ключевые особенности**

### **DWG-first подход:**
- Приоритет на получение готового DWG
- PDF только по запросу пользователя
- Никаких автоматических конвертаций

### **Блокировка:**
- Одна задача на пользователя
- Firestore с TTL 30 минут
- Автоматическое снятие при завершении

### **Forge API:**
- Только облачная обработка
- Мониторинг статуса
- Обработка ошибок и таймаутов

### **Поэтапность:**
- Четкие этапы обработки
- Интерактивный выбор PDF
- Информативные сообщения

---

## 🎉 **Результат**

**BTI-Bot успешно переписан под поэтапный DWG-first режим!**

Система теперь работает по принципу:
1. **Загрузил DWG** → получил готовый DWG
2. **Выбрал PDF** → получил PDF (опционально)
3. **Одна задача за раз** с блокировкой
4. **Только Forge API** для обработки

**Готово к деплою и тестированию!** 🚀
